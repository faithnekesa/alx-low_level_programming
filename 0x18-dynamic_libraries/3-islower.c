#include "main.h"

/**
 * _islower - checks for lowercase character
 * @alphabet: the character to check
 * Return: 1 if alphabet is lowercase, 0 otherwise
 */
int _islower(int alphabet)
{
	return (alphabet >= 'a' && alphabet <= 'z');
}
